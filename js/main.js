// This is now the entry point that imports the other modules
import * as THREE from "three";
import { CONFIG } from "./config.js";
import { Renderer } from "./renderer.js";
import "./visualization.js"; // This will automatically run the visualization

console.log("3D Color Wheel Visualization Loaded");
